#Dado un lista de números del 1 al 30, crea un programa que elimine los números impares de la lista y muestre la lista resultante.

listNum = list(range(30+1))

nuevaLista = [ ]

for i in listNum:
    if i % 2 != 0:
        nuevaLista.append(i)


print(nuevaLista)


for i in range(len(listNum)-1, -1, -1):
    if (i+1) % 2 != 0:
        listNum.pop(i)

print(listNum)
